import { NextResponse } from "next/server"; import { db } from "@/lib/db"; import { requireStudioUser } from "@/app/api/_studio-auth";
export async function GET(req:Request){try{const u=await requireStudioUser();const {searchParams}=new URL(req.url);const projectId=searchParams.get("projectId");const assets=await db.asset.findMany({where:{userId:u.id,...(projectId?{projectId}: {})},orderBy:{createdAt:"desc"},take:100});return NextResponse.json({assets});}catch{return NextResponse.json({error:"Unauthorized"},{status:401});}}
