import { NextResponse } from "next/server"; import { db } from "@/lib/db"; import { requireStudioUser } from "@/app/api/_studio-auth";
export async function GET(){try{const u=await requireStudioUser();return NextResponse.json({voices:await db.voiceProfile.findMany({where:{userId:u.id},orderBy:{updatedAt:"desc"}})});}catch{return NextResponse.json({error:"Unauthorized"},{status:401});}}
export async function POST(req:Request){try{const u=await requireStudioUser();const b=await req.json();const v=await db.voiceProfile.create({data:{userId:u.id,name:String(b.name||"Voice").slice(0,100),description:b.description||null,language:b.language||null,audioUrl:b.audioUrl||null,provider:b.provider||null,providerId:b.providerId||null,metadata:b.metadata||undefined}});return NextResponse.json({voice:v});}catch{return NextResponse.json({error:"Unauthorized"},{status:401});}}
