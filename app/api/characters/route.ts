import { NextResponse } from "next/server"; import { db } from "@/lib/db"; import { requireStudioUser } from "@/app/api/_studio-auth";
export async function GET(){try{const u=await requireStudioUser();return NextResponse.json({characters:await db.character.findMany({where:{userId:u.id},orderBy:{updatedAt:"desc"}})});}catch{return NextResponse.json({error:"Unauthorized"},{status:401});}}
export async function POST(req:Request){try{const u=await requireStudioUser();const b=await req.json();const c=await db.character.create({data:{userId:u.id,name:String(b.name||"Character").slice(0,100),description:b.description||null,imageUrl:b.imageUrl||null,style:b.style||null,referenceId:b.referenceId||null,metadata:b.metadata||undefined}});return NextResponse.json({character:c});}catch{return NextResponse.json({error:"Unauthorized"},{status:401});}}
